const { test, expect } = require('../../fixtures/fixtures.js')

test('after creating an issue via API and adding a comment via UI, the comment should be asserted via API', async ({ request, page, ids }) => {

})
